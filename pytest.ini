[pytest]
testpaths = tests
# The suite exists partly to catch deprecations early (it is how the FastAPI
# on_event removal was found), so warnings are not allowed to scroll past.
filterwarnings =
    error::DeprecationWarning
